services:
  navidrome:
    image: deluan/navidrome:latest
    user: 1001:992  # operator:operator
    ports:
      - "4533:4533"
    restart: unless-stopped
    environment:
      ND_LOGLEVEL: info
    volumes:
      - "/home/operator/navidrome/data:/data"
      - "/mnt/mediashare:/music:ro"
